# Unit Tests

Unit tests for individual modules and components.

Tests are primarily located in each source file using `#[cfg(test)]` blocks.
This directory contains additional unit test suites that span multiple modules.
